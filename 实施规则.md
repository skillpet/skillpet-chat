# 实施规则

> 所有实施计划必须遵循本文档的规则。新会话执行任何实施计划前，先读此文档。

---

## 1. 子智能体优先原则（关键！）

执行实施计划时，**必须最大程度使用子智能体（Task 工具）并行执行任务**。

### 为什么

- **速度**：子智能体并行执行比主智能体串行执行快 3~5 倍
- **上下文**：主智能体会话上下文过长会导致卡顿、遗忘、输出质量下降
- **稳定性**：每个子智能体有独立上下文窗口，不互相干扰

### 分派规则

```
拿到实施计划后：
  1. 识别当前阶段中可并行的步骤
  2. 每个独立步骤 → 启动一个子智能体（Task 工具）执行
  3. 有依赖关系的步骤 → 等前置子智能体完成后再启动
  4. 主智能体只负责：分派、收集结果、阶段审查
```

### 什么必须用子智能体

| 场景 | 分派方式 |
|------|---------|
| 修改 1 个以上文件的步骤 | 子智能体 |
| 需要读取大量代码再改动的步骤 | 子智能体 |
| 多个互不依赖的步骤 | 并行启动多个子智能体（上限 4 个） |
| 阶段审查 / 代码审查 | 子智能体 |
| 探索代码库、搜索引用 | 子智能体（subagent_type: explore） |

### 什么由主智能体直接做

| 场景 | 说明 |
|------|------|
| 更新实施计划状态标记（⬜ → ✅） | 单行改动，不值得启动子智能体 |
| 向用户汇报结果 | 必须主智能体做 |
| 分析子智能体返回的结果并决定下一步 | 调度职责 |
| 修改 1 个文件中的少量内容 | 如果改动非常小可以直接做 |

### 子智能体 prompt 规范

启动子智能体时，prompt 必须包含：
1. **目标**：明确要完成什么
2. **涉及文件**：列出需要读取和修改的文件路径
3. **具体要求**：改动的详细规格（不要只说"按计划执行"）
4. **验收标准**：完成后需要满足什么条件
5. **返回要求**：告诉子智能体完成后返回什么信息（改了哪些文件、关键改动摘要）

---

## 2. 阶段执行流程

按阶段顺序执行，不跳跃。每个阶段内的步骤按编号顺序执行。

**连续执行，禁止中断**：一个阶段审查通过后，立即进入下一阶段，不要暂停等用户确认。所有阶段全部完成后才向用户汇报结果。

```
阶段 N 开始
    │
    ├─ 分析步骤依赖关系，识别可并行的步骤
    │
    ├─ 并行启动子智能体执行各步骤（上限 4 个）
    │
    ├─ 收集子智能体结果，检查是否有失败
    │
    ├─ 有依赖的后续步骤 → 继续启动子智能体
    │
    ├─ 全部步骤完成后 → 阶段审查（见 §3，用子智能体执行）
    │
    ├─ 审查通过 → 更新文档状态标记（⬜ → ✅）
    │
    └─ 立即进入阶段 N+1（不要暂停等确认）
```

全部阶段完成后 → 总审查（见 §4）→ 向用户汇报。

---

## 3. 阶段审查

每个阶段完成后，**启动一个子智能体**执行以下审查循环：

1. **全量审查**本阶段所有改动文件
2. **只报告未通过的条目**，不要逐项列出已通过的检查项
3. 发现问题**立即修复**
4. 修复后**重新全量审查**（不是只看修复点，是所有改动文件从头审）
5. 干净的一轮**只输出一句**：**"全量审查通过，零问题"**——不要列出文件清单、不要列出检查项、不要总结改动范围
6. **循环直到一轮零问题为止**

审查范围：

- 静态错误（linter、TypeScript 类型）
- 逻辑正确性（调用路径推演、边界条件）
- 行为一致性（前后端数据格式对齐、类型对齐）
- 遗留 bug（如果审查中发现之前遗留的问题，不限于本次改造，一并修复）

> 审查子智能体的 prompt 中必须列出本阶段所有改动文件的完整路径，让子智能体有明确的审查范围。

---

## 4. 总审查

全部阶段完成后，对**所有阶段的全部改动文件**再执行一次 §3 的完整审查循环（同样用子智能体执行）。

目的：捕获跨阶段引入的不一致（如阶段 1 改了类型，阶段 3 的代码没跟上）。

---

## 5. 文档同步

每个阶段审查通过后，同步更新：

1. **实施计划**中的状态标记（⬜ → ✅）
2. **方案文档**中被实施改变的内容（如有偏差）
3. **AGENTS.md**（如果改动影响了项目结构或约定）

---

## 6. 错误处理

- 本次改动引入的错误：必须在当前阶段修复
- 发现的历史遗留错误：一并修复，不留到下一阶段
- 修复后必须重新走 §3 审查循环

---

## 7. 验收

每个阶段的验收标准写在实施计划中。审查通过 ≠ 验收通过——审查是代码层面，验收是功能层面。两者都通过才能进入下一阶段。
